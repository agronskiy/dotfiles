# Ripgrep default options
--hidden
--smart-case
# To enable set -u when manually running ripgrep
# --no-ignore
--follow
# To esable, put `(?m)` in the query.
# --multiline
--multiline-dotall
--glob=!.git/*
