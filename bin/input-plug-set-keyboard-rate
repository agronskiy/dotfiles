#!/usr/bin/env bash

# This is a script allowing to set repeat rate on linux on reattaching the keyboard in cinnamon. 
# Referenced here https://github.com/linuxmint/cinnamon/issues/11198#issuecomment-2020326604
# To use it:
# 1. Install inputplug (sudo apt install inputplug).
# 2. Add a custom startup application.
#    /usr/bin/inputplug --command input-plug-set-keyboard-rate
 
if [[ "$1" == "XIDeviceEnabled" && "$3" == "XISlaveKeyboard" ]]; then
    	xset r rate 175 40
fi
